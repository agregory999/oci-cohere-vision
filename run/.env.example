# Source this file through run/local.sh after copying it to run/.env.
# Values here are safe examples only. Never place credentials, keys, tokens, or OCIDs in this file.

# Required
OCI_COMPARTMENT_ID=""

# Optional local
OCI_CONFIG_PROFILE="DEFAULT"
OCI_CONFIG_FILE="${HOME}/.oci/config"

# Optional container
# This local-only project has no container deployment configuration.

# Generic
OCI_GENAI_REGION="us-chicago-1"
OCI_COHERE_VISION_MODEL_ID="cohere.command-a-vision"
VISION_LAB_HOST="127.0.0.1"
VISION_LAB_PORT="8501"
